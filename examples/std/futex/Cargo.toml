[package]
name = "arceos-std-futex"
version = "0.1.0"
edition.workspace = true
authors = ["ArceOS Contributors"]

[dependencies]
arceos-rust = { workspace = true, default-features = true, features = ["sync", "multitask", "irq", "log-level-debug"] }
libc = "0.2"
